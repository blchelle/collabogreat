import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
	errorBanner: {
		backgroundColor: '#e81410',
		height: '2rem',
	},
	dismissButton: {
		marginLeft: 'auto',
	},
});
